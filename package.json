{
  "name": "copilot-sales",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "electron:dev": "concurrently -k \"vite\" \"wait-on http://localhost:5173 && electron . --vite\"",
    "electron": "electron .",
    "electron:build": "vite build && electron-builder --win --x64",
    "electron:pack": "vite build && electron-builder --dir"
  },
  "build": {
    "appId": "com.copilot-sales.app",
    "productName": "CoPilot Sales",
    "files": [
      "dist/**/*",
      "electron/**/*"
    ],
    "directories": {
      "output": "release"
    }
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "electron": "^36.3.1",
    "svelte": "^4.2.0",
    "vite": "^5.0.0",
    "wait-on": "^9.0.10",
    "ws": "^8.20.0"
  },
  "dependencies": {
    "docx": "^9.7.0",
    "dompurify": "^3.4.6",
    "electron-audio-loopback": "^1.0.6",
    "file-saver": "^2.0.5",
    "marked": "^18.0.4"
  }
}
